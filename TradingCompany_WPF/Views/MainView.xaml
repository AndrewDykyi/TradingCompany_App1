<UserControl x:Class="TradingCompany_WPF.Views.MainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="350" Width="400">
    <Grid Background="#f0f0f0">
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <Button Content="Products" Command="{Binding OpenProductWindowCommand}" Width="280" Height="50" Margin="10"/>
            <Button Content="Categories" Command="{Binding OpenCategoryWindowCommand}" Width="280" Height="50" Margin="10"/>
            <Button Content="Logout" Command="{Binding LogoutCommand}" Width="280" Height="50" Margin="10" Background="Red" Foreground="White"/>
        </StackPanel>
    </Grid>
</UserControl>